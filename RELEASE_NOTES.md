# v1.8.3 - Patch

## Fixed

- **All dependency advisories resolved** - Remaining root-level advisories in `@xmldom/xmldom` (XML injection, DoS) and `brace-expansion` (DoS) are now cleared. Both updated within existing semver ranges via lock file only. The project is now fully clean across both root and server dependency trees.
